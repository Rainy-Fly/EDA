# What is this?
在构建项目时，大家会遇到各类问题，解决后可以写入这个文件，更好地帮助组员
# 问题汇总
## CMakeLists.txt修改后编译报错   *9月3日*
由于`cmake --build build`只是读取build/Makefile的配置来编译的，当你修改项目的信息并在CMakeLists.txt中同步时，
Makefile并不会更新，会导致与现状冲突。因此，应该先运行`cmake -B build`,重新生成Makefile。放心，他不会动wxWidgtes的。
之后只要不同CMakeLists.txt,直接`cmake --build build`就能编译.
